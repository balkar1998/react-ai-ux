import React from 'react'
import { AITableProps } from './types'

export function AITable({ data, columns, className }: AITableProps) {
  return (
    <div className={className}>
      <p>AITable coming soon</p>
    </div>
  )
}